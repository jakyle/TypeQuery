import {assert} from 'chai';
import 'mocha';
import {ReferenceItem} from '../classes/ReferenceItem';

/* const ref = new ReferenceItem('Facts and Figures', 2016);

describe('referenceItem', () => {
    describe('item', () => {
        it('should log correct title, year, research', () => assert
            .deepEqual(ref.item(), [
                'Facts and Figures was published in 2016.',
                'Department: Research',
            ]
        ));
    });
    describe('year change', () => {
        it('should be 2001', () => assert.equal(ref.year, 2016));
    });
    describe('publisher', () => {
        ref.publisher = 'random data publishing'
        it('should return uppercase publisher', () => assert
            .equal(ref.publisher, 'RANDOM DATA PUBLISHING'))
    });
});
 */
